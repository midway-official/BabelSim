field C
{
    type scalar
    location cell
    internal uniform (0)
    boundary
    {
        inlet { type fixedValue value (1) }
        outlet { type zeroGradient }
        lower { type symmetry }
        upper { type symmetry }
        front { type symmetry }
        back { type symmetry }
    }
}
